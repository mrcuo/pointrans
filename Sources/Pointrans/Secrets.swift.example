// Local DeepSeek API key for the built-in AI translation.
//
// Copy this file to Secrets.swift and fill in your key. Secrets.swift is
// git-ignored, so your key never lands in the repository or the GitHub repo.
enum Secrets {
    static let deepSeekApiKey = "sk-xxxxxxxxxxxxxxxxxxxxxxxx"
}
